Examples
========

Launch the Orange canvas with the example Addon

.. code-block:: bash

    python -m ewoksorange.canvas --with_example

or alternatively install the example Addon and launch the Orange canvas normally

.. code-block:: bash

    python -m pip install ewoksorange/tests/examples/ewoks_example_add
    python -m Orange.canvas
